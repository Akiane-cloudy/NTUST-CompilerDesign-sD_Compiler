void main(){
    int ai, bi ,ci, di, arr[100];
    float af, bf, cf, df;
    double ad, bd, cd, dd;
    char ac, bc, cc, dc;
    string as, bs, cs, ds;

    print arr[((((ai + bi)++ + 1) ++ - 1)-- + (((ai + bi)++ + 1) ++ - 1)--) * 2];
    arr * 2; // Warning with different type.
}